{
  "name": "theworkspacepro",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/merge-legacy-public.mjs",
    "check": "astro check",
    "validate": "node scripts/validate-public-site.mjs dist",
    "test": "npm run build && npm run validate && node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.20.1",
    "astro": "^7.1.3",
    "serve": "^14.2.6",
    "typescript": "^5.9.3"
  }
}
